# Pipe a function (pass it to another function) other words higher order function takes a lambda function

### To run this app
clone it :
```
git@github.com:codewars7/Pipe-a-function.git
```

and run a command in your terminal
```
node index.js
```
